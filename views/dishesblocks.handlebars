	{{!-- {{burger_name}}

	<button class="change-devoured btn btn-info" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour it!{{else}}Remake it!{{/if}}
	</button>

	<button class="delete-burger btn btn-danger" data-id="{{this.id}}">X</button> --}}

<h5>{{title}}</h5>
<p>{{body}}</p>
